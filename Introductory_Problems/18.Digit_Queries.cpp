#include <iostream>
#include <cmath>
#include <string>

using namespace std;
typedef long long ll;

int main()
{
	ll q;
	ll k;
	cin >> q;
	for(ll i = 0; i < q; i++)
	{
		cin >> k;
		if(k < 9) cout << k;

		ll len = 1;
		while(9 * pow(10, len-1) * len < N){
			k -= 9 * pow(10, len-1) * len;
			len++;
		}

		string S = w
	}
}
